{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Bounce",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Build/Bounce.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
            "externalConsole": true,
            "preLaunchTask": "Build Bounce (Debug)"
        }
    ]
}